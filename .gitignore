# Build output
/bin/

# Coverage
coverage.out
coverage.html

# Local loop state, if anyone points a config at the repo
/state/
release/

# Agent scratch workspace
/.superpowers/

# A local build artifact, if one lands at the root
/agent-utils
